set_schedule:
  name: Set mowing schedule (day)
  description: >-
    Overwrite the mowing plan for one weekday: enable/disable it and provide one
    or more time periods, each optionally restricted to specific zones. This
    replaces that weekday's schedule.
  fields:
    device_id:
      name: Mower
      description: Target mower (only required if more than one is configured).
      required: false
      selector:
        device:
          integration: navimower
    day:
      name: Day
      description: Weekday to program.
      required: true
      selector:
        select:
          options:
            - monday
            - tuesday
            - wednesday
            - thursday
            - friday
            - saturday
            - sunday
    enabled:
      name: Enabled
      description: Whether the mower mows on this day.
      default: true
      selector:
        boolean:
    periods:
      name: Periods
      description: >-
        List of time windows. Each item: start "HH:MM", end "HH:MM", and an
        optional zones list of region ids (empty = all zones). Multiple items =
        multiple mowing sessions that day.
      required: false
      example: '[{"start": "09:45", "end": "17:15", "zones": []}]'
      selector:
        object:

mow:
  name: Mow now
  description: >-
    Start mowing immediately: the chosen zones, in the order given, with a
    restart-vs-continue choice.
  fields:
    device_id:
      name: Mower
      description: Target mower (only required if more than one is configured).
      required: false
      selector:
        device:
          integration: navimower
    zones:
      name: Zones
      description: >-
        Region ids to mow, in the order they should be mowed (e.g. [5,1] mows
        region 5 first). Leave empty to mow all zones and let the mower choose
        its own route.
      required: false
      example: "[1]"
      selector:
        object:
    reset:
      name: Restart from scratch
      description: >-
        On = "Cancella i progressi e riparti da zero" (re-mow the whole zone).
        Off = continue, mowing only the area not yet cut.
      default: true
      selector:
        boolean:

export_diagnostics:
  name: Export diagnostics
  description: >-
    Run all known read-only private-cloud queries and export sanitized raw data,
    nested key paths, keyword matches and the passive MQTT topic/key inventory.
    No mower command, settings write or map edit is made. The existing client may
    automatically reauthenticate if its stored session has expired.
  fields:
    device_id:
      name: Mower
      description: Target mower (only required if more than one is configured).
      required: false
      selector:
        device:
          integration: navimower
    include_compressed_map:
      name: Include compressed map request
      description: >-
        Also request the compressed map endpoint. Large binary/base64 content is
        represented only by length and SHA-256 in the output.
      default: true
      selector:
        boolean:
